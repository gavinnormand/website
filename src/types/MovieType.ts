export interface MovieType {
  title: string;
  link: string;
  watchedDate: string;
  rewatch: string;
  filmYear: string;
  rating: number;
  liked: boolean;
  description: string;
  posterURL: string;
}
